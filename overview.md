# 四上学习帮 · 本次更新概览

## 一、游戏"进不去"问题处理与导航加固
- 路由 `/games/:subject/:gameId`（name: `GamePlay`）本身正确，编译无错；原"进不去"极可能为上一会话 dev server 已停止。
- **游戏卡片改为原生 `<router-link>`**（学科游戏 / 最近游玩 / 今日推荐三个入口），用命名路由 `GamePlay` 跳转，彻底消除点击处理器的边界问题，并重置了 `<a>` 默认下划线样式。
- **`GamePlayView.vue` 增加兜底**：当游戏配置缺失时显示"未找到该游戏 + 返回游戏中心"，不再出现空白页。
- **`useGameEngine.ts` 增加 param 监听**：在组件被复用时重新加载题目并重置状态（连击/计时/进度等）。

## 二、16 个游戏题库大幅扩充（用户反馈"太少了"）
数据文件：`src/data/games/{chinese,math,english,science}.ts`

| 学科 | 游戏 | 扩充前 → 后 |
|---|---|---|
| 语文 | 古诗填字 / 文学常识 / 成语接龙 / 词语配对 | 6→12 / 12→26 / 10→20 / 6→18 |
| 数学 | 口算 / 角度 / 算式 / 图形分类 | 20→40 / 10→17 / 8→16 / 8→14 |
| 英语 | 单词配对 / 听音选词 / 句子排序 / 押韵 | 15→48 / 8→18 / 8→16 / 8→16 |
| 科学 | 声音连线 / 食物分类 / 器官拼图 / 力的实验 | 7→12 / 12→22 / 6→11 / 8→15 |

## 三、验证
- `npm run build`（vue-tsc 类型检查 + vite 打包）通过。
- `npm run dev` 已启动：http://localhost:5173 （局域网 http://192.168.11.0:5173 ），HTTP 200 可访问。

> 说明：本次未使用自动化浏览器实测点击（环境无可用 headless 浏览器），但已通过类型检查、打包成功、dev server 200，以及原生 router-link 跳转保证导航可靠。建议在手机/浏览器打开上面的地址进入"放松游戏"实测。
