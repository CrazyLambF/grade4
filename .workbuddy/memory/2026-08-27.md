# 2026-08-27 工作记录

## 任务：修复游戏进入问题 + 扩充 16 个游戏题库

### 游戏进入（导航）问题
- 排查 `GameCenterView.vue` → `goGame` → `router.push('/games/:subject/:gameId')`，路由定义 `/games/:subject/:gameId` 名称 `GamePlay` 正确，编译无错。
- 将游戏卡片（学科游戏、最近游玩、今日推荐）由 `@click` 改为原生 `<router-link :to="{ name:'GamePlay', params:{subject, gameId} }">`，消除点击处理边界问题，并重置 link 样式（`text-decoration:none; color:inherit`）。
- `GamePlayView.vue` 增加 `v-else` 兜底：配置缺失时显示"未找到该游戏"+"返回游戏中心"，避免空白页（原"进不去"可能表现为空白）。
- `useGameEngine.ts`：将 `gd` 改为 `ref`，新增 `watch(route.params.gameId)` 在组件复用时重载题目并重置状态；同步修正 `sortCats/sortKey/sortInst` 引用为 `gd.value.*`（否则 build 报错）。
- 结论：编译、构建均通过；`npm run dev` 启动于 http://localhost:5173 返回 200。原"进不去"极疑似上一会话 dev server 已停止，已重启验证。

### 题库扩充（用户反馈"太少了"）
大幅扩充四个学科游戏数据文件 `src/data/games/{chinese,math,english,science}.ts`：
- 语文：古诗填字 6→12 首；文学常识 12→26；成语接龙 10→20；词语配对 6→18。
- 数学：口算 20→40；角度 10→17；算式 8→16；图形分类 8→14。
- 英语：单词配对 15→48；听音选词 8→18（去重选项）；句子排序 8→16；押韵 8→16。
- 科学：声音连线 7→12；食物分类 12→22；器官拼图 6→11；力的实验 8→15。
- 注意：`organ-puzzle` 引擎为 order，单元素排列；`shapeSort`/`foodSort` 分类沿用原 sortCats。

## 任务：语文背诵模块趣味化重构

### 改动文件
- `src/data/chinese/recitations.ts`：接口新增 `dynasty`/`difficulty`(1-3)/`stars` 字段、新增 `background`(创作背景)；新增《鹿柴》《嫦娥》两首诗。
- `src/views/chinese/RecitationView.vue`：完全重写。

### 新功能
1. **顶部进度横幅**：渐变色横幅显示总星星数/最大星数。
2. **Tab1 诗歌卡片（翻转）**：每首诗一张渐变色卡片，正面显示诗名/作者/朝代/难度/星星，点击 3D 翻转看全文+译文+创作背景+"去闯关"按钮。
3. **Tab2 背诵闯关（三关递进）**：
   - Level 1「看文背诵」：展示全文→点"我记好了"→随机隐藏部分诗句(半透明)→凭记忆→通过得 1 星。
   - Level 2「填空背诵」：自动从每句中挖空一个语义段，输入答案，即时正误反馈(绿/红)，全对得 2 星。
   - Level 3「全默写」：逐句输入整首诗，全对得 3 星。
   - 闯关进度条(3 步连线)，成功弹窗(星星动画)。
   - 星星进度用 localStorage 持久化(`recite_stars`)。
4. **Tab3 诗句排序**：打乱诗句顺序，点选排列，确认检查。
5. **视觉**：8 种渐变背景配色、3D 卡片翻转、KaiTi 楷体诗文字体、进度条/步骤指示器、成功弹窗。
- `npm run build` 通过；RecitationView 从 6.48KB 增至 15.87KB。

## 任务：每日计算改为随机题目生成器

### 改动文件
- 新建 `src/utils/mathQuestionGenerator.ts`：四上各单元随机题目生成器。
- 重写 `src/views/math/DailyCalcView.vue`：使用新生成器，增加题型/难度/数量选择。

### 生成器覆盖题型
- **综合口算**：随机抽各类型 + 基础四则。
- **大数认识**（Unit 1）：读数（数字转中文）、比较大小、四舍五入到万、改写万/亿。
- **面积换算**（Unit 2）：m²↔公顷、公顷↔km²、km²↔公顷。
- **角的度量**（Unit 3）：判断角类型、角度计算(×n)、钟面角、平角/周角关系。
- **乘法运算**（Unit 4）：三位数×两位数、积的变化规律、乘法估算。
- **除法运算**（Unit 6）：口算除法(整十数)、笔算除法(确保整除)、有余数除法。
- **混合运算**：加减混合、乘加、乘减、除乘、带括号。
- 每题型按难度 1/2/3 分层；除法确保整除（genDiv 先选除数×商=被除数）；减法确保非负。
- 去重 + 打乱顺序。

### DailyCalcView 新功能
- 设置页：7 种题型网格选择、3 档难度、4 档题量(10/15/20/30)。
- 答题页：进度条(含填充条)、题型标签、连击计数器(≥3 显示🔥)、计时(每题 15 秒)。
- 结果页：正确率/用时统计、最高连击、错题回顾列表(题目/你的答案/正确答案)。
- `npm run build` 通过。
